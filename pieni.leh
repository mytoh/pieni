

(install
  (
    (run-process `(gauche-install -T ,(build-path ( *lehti-dist-directory* ) "pieni")
                                  "bin/pieni")
                 :wait #t)
   ;; library sources
   (copy-directory* (build-path ( *lehti-cache-directory* ) "pieni/lib")
                    (build-path ( *lehti-dist-directory* ) "pieni/lib"))
)
)
