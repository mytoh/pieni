

(install
  (
    (run-process `(gauche-install -T ,(build-path ( *lehti-dist-directory* ) "pieni")
                                  "bin/pieni")
                 :wait #t)
   ;; library sources
   (copy-directory* (build-path ( *lehti-cache-directory* ) "pieni/src")
                    (build-path ( *lehti-dist-directory* ) "pieni/src"))
)
)
